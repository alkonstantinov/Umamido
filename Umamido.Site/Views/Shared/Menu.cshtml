
@{
    string menuClass = "";


}
<ul id="primary-menu" class="menu nav-menu" aria-expanded="false">
    @if (@ViewContext.RouteData.GetRequiredString("action").ToLower() == "order")
    {
        menuClass = "menu-item current-main";
    }
    else
    {
        menuClass = "menu-item";
    }
    <li id="menu-item-101" class='@menuClass'>
        @Html.ActionLink(@Umamido.Resources.Resources.Order, "Order", "Home")        
    </li>

    @if (@ViewContext.RouteData.GetRequiredString("action").ToLower() == "aboutus")
    {
        menuClass = "menu-item current-main";
    }
    else
    {
        menuClass = "menu-item";
    }
    <li id="menu-item-462" class='@menuClass'>
        <a href="about.html">@Umamido.Resources.Resources.AboutUs</a>
    </li>
    @if (@ViewContext.RouteData.GetRequiredString("action").ToLower() == "restaurants" ||
                @ViewContext.RouteData.GetRequiredString("action").ToLower() == "restaurant")
    {
        menuClass = "menu-item current-main";
    }
    else
    {
        menuClass = "menu-item";
    }

    <li id="menu-item-101" class='@menuClass'>
        @Html.ActionLink(@Umamido.Resources.Resources.Restaurants, "Restaurants", "Home")

    </li>
    @if (@ViewContext.RouteData.GetRequiredString("action").ToLower() == "blog")
    {
        menuClass = "menu-item current-main";
    }
    else
    {
        menuClass = "menu-item";
    }


    <li id="menu-item-382" class='@menuClass'>
        @Html.ActionLink(@Umamido.Resources.Resources.Blog, "Blogs", "Home")
    </li>

    @if (@ViewContext.RouteData.GetRequiredString("action").ToLower() == "contactus")
    {
        menuClass = "menu-item current-main";
    }
    else
    {
        menuClass = "menu-item";
    }
    <li id="menu-item-21" class='@menuClass'>
        @Html.ActionLink(@Umamido.Resources.Resources.Contacts, "ContactUs", "Home")
        
    </li>
</ul>
